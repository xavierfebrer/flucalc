name: flucalc
description: A calculator made with flutter.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.3+4

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hack2s_flutter_util:
    path: ../../hack2s_flutter_util/hack2s_flutter_util

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true